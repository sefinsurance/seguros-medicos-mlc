{
  "name": "obamacarelocal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "echo 'Cloudflare Pages deploy is automatic (no deploy script needed).'"
  },
  "dependencies": {
    "astro": "^5.10.1"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.2.1"
  }
}
