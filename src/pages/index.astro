---
// Root homepage. Renders the rich English homepage (converted from React to Astro);
// canonical points to /en/ so the language-specific URL is the indexed one
// (the Spanish counterpart is /es/).
import RichHome from "@/components/home/RichHome.astro";
---
<RichHome lang="en" canonical="https://obamacarelocal.com/en/" />
