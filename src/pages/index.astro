
---
const accept = Astro.request.headers.get("accept-language") || "";
const prefersEs = /^es\b/i.test(accept) || /,\s*es\b/i.test(accept);
return Astro.redirect(prefersEs ? "/es/" : "/en/", 302);
---
