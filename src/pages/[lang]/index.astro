---
// Rich homepage (converted from the old React /) at /en/ and /es/.
import RichHome from "@/components/home/RichHome.astro";

export function getStaticPaths() {
  return [{ params: { lang: "es" } }, { params: { lang: "en" } }];
}
const { lang } = Astro.params;
---
<RichHome lang={lang} />
