
---
const { tone="light", id, title, subtitle } = Astro.props;
---
<section class={`sec ${tone}`} id={id}>
  <div class="container">
    {title && <h2 class="title">{title}</h2>}
    {subtitle && <p class="sub">{subtitle}</p>}
    <slot />
  </div>
</section>
