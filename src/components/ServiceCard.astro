---
const { title, subtitle, icon, href = "#" } = Astro.props;
---
<a class="card" href={href}>
  <div class="card__icon">
    <img src={icon} alt="" />
  </div>
  <div class="card__title">{title}</div>
  <div class="card__sub">{subtitle}</div>
</a>

<style>
.card{
  display:block;
  background: rgba(255,255,255,0.78);
  border-radius: 22px;
  padding: 18px 18px 16px;
  box-shadow: var(--shadow-soft);
  border: 1px solid rgba(18,50,79,0.06);
  transition: transform .18s ease, box-shadow .18s ease;
  text-align:center;
}
.card:hover{
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
.card__icon{
  width:72px;
  height:72px;
  border-radius: 18px;
  margin: 0 auto 12px;
  display:grid;
  place-items:center;
  background: radial-gradient(circle at 30% 25%, rgba(255,255,255,.95), rgba(220,236,255,.55));
  border: 1px solid rgba(18,50,79,0.08);
}
.card__icon img{ width:56px; height:56px; }
.card__title{ font-weight:900; letter-spacing:-.2px; }
.card__sub{ margin-top:6px; font-size:13px; color: var(--muted); line-height:1.2; }
</style>
